# C8 FINAL SEND PROMPT — self-contained brief (attach this file as attachment #1)

You are **Consultant C8**, the expert who designed the finisher iterations
V1–V7 and the coherent stage one. In this round you act as an **unbiased,
independent, expert professional** under direct owner oversight. The
operator's standing demand: *"best effective optimisation and systemisation
for optimal performance, results and quality."*

**Your only inputs are: (a) this file, (b) the ≤19 image attachments listed
in §0, (c) your memory of previous rounds.** Anything else you need that is
not present — SAY SO and STOP that thread. Never invent data. If an
attachment listed in §0 is missing, state it first thing.

---

## 0. YOUR ATTACHMENT MANIFEST (exactly 20 slots — check them off)

1. **This prompt file.**
2. **Sheet-OG** — one grid image: all 12 ORIGINAL photos (numbered 1–12,
   same numbering as the tables below).
3. **Sheet-A** — one grid image: the 11 Config A remint outputs, labeled
   F1–F11 with their pair numbers (see §1 mapping).
4. **Sheet-1A** — one grid image: the 11 Config 1A remint outputs, same
   labels.
5. **Full-res pair #10** — OG-10 and A-10 (the CLEAR win).
6. **Full-res pair #3** — OG-3 and A-3 (wash-proof pure swap).
7. **Full-res pair #4** — OG-4 and A-4 (wash-proof fail).
8. **Full-res pair #9** — OG-9 and A-9 (Config 1A catastrophic — source
   fingerprint survives).
9. **Full-res pair #1** — OG-1 and A-1 (Config 1A catastrophic).
10. **Full-res pair #6** — OG-6 and A-6 (Config 1A rescued this row).
11. **Full-res pair #5** — OG-5 and A-5 (Config 1A rescued this row).
12. **Full-res pair #7** — OG-7 and A-7 (near-clear under A).
13. **Full-res pair #8** — OG-8 and A-8 (borderline under A).

Slots 14–20 are the operator's choice of the remaining full-res files
(OG / A / 1A) — prefer 1A remints of #6/#5 (the rescues) and any OG whose
scene class is not covered by pairs #1–#10 above. If fewer than 20 items
arrive, state the count and work with what exists.

**Numbering law:** OG files are `CFA-REAL-CREATOR-IMG-<n>`. Config A
remints are `SEQ-CFA-<hash>` dispatched per §1's file-number mapping.
Config 1A remints are `SEQ-1A-<hash>`. The file NUMBER inside the remint
name (F1…F11) is what links a remint to its OG — see §1. Trust the
mapping below over any filename guess.

---

## 1. THE TWO PAIRED TESTS — exact numbers (this replaces everything you
remember about these rows)

**File mapping (owner-provided, per-image):** OG 12 → (remint not
recorded) · OG 11 → F1 · OG 10 → F2 · OG 9 → F3 · OG 8 → F4 · OG 7 → F11 ·
OG 6 → F5 · OG 5 → F6 · OG 4 → F7 · OG 3 → F8 · OG 2 → F9 · OG 1 → F10.
The same mapping applies to both the Config A and Config 1A batches.

### Config A (qwen wash) — scoreboard: 2 clear · 2 near · 1 border · 6 fail

| # | OG AI% | A remint | A residual families | verdict |
|---|---|---|---|---|
| 11 | 99.2 | 99.1 | wan 70.3 / flux 9.2 | FAIL (no-op) |
| 10 | 99.9 | 0.5 | firefly 0.2 | CLEAR |
| 9 | 99.9 | 11.0 | gemini3 3.7 / flux 2.5 | NEAR |
| 8 | 99.9 | 24.9 | flux 22.3 | BORDER |
| 7 | 99.9 | 11.5 | flux 15.5 | NEAR |
| 6 | 99.9 | 51.2 | flux 74.4 | FAIL |
| 5 | 99.9 | 83.2 | wan 78.0 | FAIL |
| 4 | 99.9 | 91.1 | flux 35.4 / kling 28.1 | FAIL |
| 3 | 99.9 | 99.9 | SD 73.5 / kling 19.6 | FAIL (pure swap) |
| 2 | 99.9 | 96.3 | other 35.3 / kling 16.7 | FAIL |
| 1 | 99.9 | 8.9\* | ernie 37.5 | NEAR\* |

(\* #1 headline 8.9% conflicts with its own breakdown 78.9% — unresolved.)

### Config 1A (qwen+zimage wash — ONE variable moved) — scoreboard:
1 clear · 1 near · 2 border · 7 fail. Head-to-head: Config A wins 6 pairs,
Config 1A wins 4, 1 tie. **Config A stays the default.**

| # | 1A remint | 1A residual families | vs Config A |
|---|---|---|---|
| 11 | 97.3 | ernie 58.1 / imagen4 21.8 | 1A marginal, both fail |
| 10 | 1.1 | gemini3 0.9 | A, both clear |
| 9 | 83.9 | **gemini3 93.5** | A — 1A CATASTROPHIC |
| 8 | 67.5\* | flux 33.4 / 4o 16.1 | A (\* cached "24.9" headline repeats) |
| 7 | 19.0 | flux 23.9 | A |
| 6 | 15.5 | flux 23.6 | **1A — flips FAIL→BORDER** |
| 5 | 12.3 | gemini3 5.5 / flux 5.4 | **1A — flips FAIL→NEAR** |
| 4 | 99.9 | gemini 55 / kling 21.6 | A, both fail |
| 3 | 99.9 | stablecascade 64.6 / SD 18.7 | tie — both pure swap |
| 2 | 82.3 | kling 41.9 / other 13.3 | 1A, both fail |
| 1 | 99.6 | **ernie 80.6** | A — 1A CATASTROPHIC |

**Oracle** (better wash chosen per image): 2 clear · 3 near · 2 border ·
4 fail — rescues #5 and #6 from the FAIL column. **Probe-routed wash
selection is the proven architecture.**

---

## 2. SETTLED FACTS (do not re-argue without new data)

- **F1 — Wash mechanism.** qwen = RELIABLE breaker, poor de-stamper
  (failures always carry a NEW family: wan/flux/kling/SD). qwen+zimage =
  UNRELIABLE breaker (where it fails hard the SOURCE fingerprint survives:
  gemini3 93.5 on #9, ernie 80.6 on #1) yet wins on #6/#5. Wash efficacy is
  per-CONTENT, not per-source.
- **F2 — Wash-proof rows:** #11, #3, #4, #2 fail ≥ 82% under BOTH washes.
  They need the non-generative path (no regen → no re-stamp) or manual QA.
  The non-generative path does not regenerate or resample — it may preserve
  MORE of the source's HD crispness.
- **F3 — The 360p quality gap is the stage-one lattice.** Chain in order of
  magnitude: HD source → single-Lanczos ≤1250px → q92 4:2:0 (≈84–96% of
  pixels discarded BEFORE the wash runs) → 1536-capped regen → finisher
  `strong` + smoothing 1.25× removes the surviving high-frequency detail.
  Config 1A changed nothing quality-side (outputs measured "darker and
  lower resolution"). The finisher cannot restore discarded detail.
- **F4 — Requested ≠ executed.** Config A requests `strength: deep` +
  adaptive, but the frozen V8.9 engine's adaptive ladder is
  `["light","balanced"]` — **deep is RETIRED** from it. The sequence path's
  adaptive finish probes `["strong","standard"]` and picks per-image with
  the internal detector. A settings-code filename proves what was
  REQUESTED, not what EXECUTED.
- **F5 — Runtime-only levers in the frozen engine:** `wash_model ∈ {qwen,
  zimage, qwen+zimage}`, `zimage_denoise` 0.05–0.3, `route_by_baseline`,
  `strength` (template only), `deep_degrade_scale`, `color_restore`,
  `output_target` 256–8192, jpeg settings. Track F may choose WHICH and in
  WHAT ORDER — never their implementation.
- **F6 — Frozen ship-gate:** ai ≤ 0.45 AND flux-family ≤ 0.30 AND deepfake
  ≤ 0.10. The internal detector's night-content verdicts are UNPROVEN and
  must be calibrated against both external vendors before they may drive
  routing.
- **F7 — Finisher constants (V6):** `POLISH_G_MIN=(0.40,0.55,0.75)`,
  `POLISH_TAU=(0.004,0.007,0.010)`, `WALL_DITHER_Y=0.13/255`,
  `WALL_DITHER_C=0.04/255`, wall RMS bright 0.32 / dark 0.55 LSB, chroma
  0.08 LSB, structure gate P>0.75 → gain ≥ 0.9, `QC_SSIM_FLOOR=0.90`,
  `QC_RHO1_MAX=0.40`, `QC_RESIDUAL_RMS_MIN=0.15/255`, `REF_TDR_FLOOR=0.60`,
  one Q97 4:4:4 encode, CPU-only, ~340–670 ms.
- **F8 — Settings-code scheme:** `SEQ-CFA-<hash>` = exact Config A;
  `SEQ-1A-<hash>` = exact Config 1A; else
  `SEQ-{CON|STD|STR|FID}-{scale}-{M0|M1}-<hash>`. `materialClean` is still
  DROPPED by both serializers in `src/lib/deepcleanClient.ts` (M1/M0 A/B
  impossible through the UI until fixed).
- **F9 — Corpus content class:** night/twilight exterior lighting product
  photography — bollard path lights, wall-wash scallop beams on rendered/
  brick walls and fences, spot lights on foliage, deep-blue skies, glowing
  windows. All 12 OGs read 99.2–99.9% AI (gemini/gemini3/ernie/imagen4) —
  this is a DE-STAMP product.
- **F10 — Data gaps that BLOCK conclusions:** G2 vendor missing on all
  files; #8's "24.9%" headline is a grader-UI cache artifact; #1's
  headline/breakdown conflict; worker reports not yet archived (so which
  wash truly executed is unverified). None answerable by code — protocol
  failures.

---

## 3. YOUR MANDATE

1. Answer the open questions in §4 with exact numbers and pseudo-code.
2. Re-rank the strategies from the tables (single static config vs routed
   vs non-generative-first) — the data already settles most of this; your
   job is thresholds and the pre-wash prediction signal.
3. Return the build order for `deepclean-worker/quality_finish.py`,
   `deepclean-worker/worker.py` routing, and `src/lib/deepcleanClient.ts`,
   plus the exact split-test data still needed.
4. Every recommendation carries a **falsifiable prediction**: direction,
   magnitude, confidence %. Owners score predictions; calibration is
   tracked. No prediction → rejected.
5. **Budget:** the entire V8 validation fits ≤ 40 graded runs (2 external
   vendors). State each experiment's run cost; rank-cut if over budget.
6. **Sandbox:** you PROPOSE, owners EXECUTE. Runtime-only routing, frozen
   engine code, report-only diagnostics, owner-approved gates. Nothing
   ships without a registry A/B.

## 4. THE OPEN QUESTIONS (answer with exact numbers and pseudo-code)

**Track F — detection (PRIMARY):**
1. Why does Config A clear 3/3 rendered walls but 2/11 real photos?
   Separate content-difficulty from wash-re-stamp; give the minimal
   ablation (wash-only / camera-only on 3 fails + 2 clears) and the exact
   decision rule assigning each failure to a cause.
2. The wash-variant ladder. Specify the adaptive ladder over (strength ×
   wash_model) with exact thresholds, ordering, and the escalation rule
   when the post-wash probe reads a stamp-family dominant. Which source
   families count as "stamp", at what share threshold, on which detector?
3. The de-stamp decision surface — routing table: OG probe value → route.
   Include non-generative-only for inputs already reading photographic;
   full regen for heavy stamps; the fallback when every wash variant
   re-stamps. Every branch states what ships and what gets the manual-QA
   flag. (Becomes `ROUTING_V1` in the worker report.)
4. Fingerprint-swap quantification: define `swap_index` (share of AI% from
   families ABSENT in OG top-3) and `retention_index` (share from families
   PRESENT in OG top-3); compute both for all 22 rows. Which best predicts
   Δ? Can the FINISHER suppress a fresh post-regen stamp, or is re-stamping
   irreducible without a stage-one debate?
5. The WAN-family night hypothesis: is there a measurable spatial/spectral
   signature the finisher can target regionally (correlated chroma noise in
   the dark-blue sky band, block artifacts on beam gradients)? Give per-ROI
   metric targets and the kill-or-confirm experiment.
6. Internal-gate calibration: same files, internal detector vs both
   external vendors. What agreement statistic + threshold must hold before
   the internal probe may drive routing on night content?
7. Pre-wash prediction signal: what OG-probe feature separates the rows
   where qwen wins (#9, #7, #1) from the rows where qwen+zimage wins (#6,
   #5)? If none exists, say so honestly — the answer is then
   both-candidates-plus-probe at the cost of one extra wash pass.
8. Wash-proof routing: the exact detection rule sending rows that fail
   ≥82% under both washes to the non-generative profiles (camera_relife /
   max_optimised), with QC gates and the manual-QA flag. Quantify the
   expected quality gain of skipping regen.

**Track Q — the 360p complaint:**
9. The lattice experiment — the single highest-value run. Specify:
   `output_target ∈ {1250, 1600, 2000}` × `wash_model ∈ {qwen,
   qwen+zimage}` on the 20-image registry (12 pairs + 3 rendered walls +
   5 real photos), both vendors + the 100%-zoom rubric (grain visibility,
   edge crispness, banding, chroma blotch, premium 1–5). Acceptance to
   raise the default lattice: (a) no registry row regresses a verdict
   CATEGORY at either vendor vs the 1250 baseline, AND (b) median rubric
   improves ≥ 1 point. Predict the outcome per row (V4's 2000px regression
   happened on the OLD stack; must be re-proven on the CURRENT stack).
10. The night-scene class treatment: per region — sky gradients, warm
    scallop beams, specular cores, dark foliage — what may be smoothed to
    what target, what must NOT be touched, mask definitions, named QC gates
    with thresholds.
11. The grain budget at 1250px: is the complaint amplitude, correlation, or
    scale? Give the three separating measurements (per-luma RMS in the
    0–0.25 band, rho1 on sky, correlation length) and the "modern phone"
    target bands.
12. Does the smoothing multiplier reach the visible grain band (H0 vs H1 vs
    H2)? Measure per-band RMS reduction vs multiplier and state which stage
    must own the grain (stage-1 injection vs finisher floor vs dither).
13. Preset collapse: final preset set (2 or 3) and the measurable
    difference (named QC metric + threshold) a preset must produce to
    justify existing.
14. The V8 finisher spec — BUT DEFER the build until the lattice lands
    (all targets rescale with it). Specify anyway: night region map,
    per-region polish targets (sky `POLISH_G_MIN` → (0.30,0.45,0.60),
    dither 0.20/0.06; beams gain ≥ 0.9; foliage floor 0.55/0.30 LSB;
    speculars excluded), per-luma wall curve (0.55/0.40/0.32/0.25 LSB),
    correlation-length cap ≤ 1.5 px, named QC gates (`qc.night_sky`,
    `qc.night_beams`, `qc.night_foliage`, `qc.night_specular`).
15. Auto-selection for beta: routed config with settings-code provenance,
    or fixed config? Give the exact rule and thresholds; state what must be
    true for a fixed default to return.

**Top five concrete changes ranked** across all tracks, each with its
trade-off, its matrix experiment, and its run cost. Flag the single largest
expected visible win for quality and the single largest expected win for
detection, separately.

---

## 5. SYSTEMIZATION — the seven laws (quote them verbatim in your answer)

1. **Settings-code law** — no export without a settings-code filename; no
   grade recorded without the filename.
2. **Executed-not-requested law** — every corpus export archives its worker
   report (attempts, wash model, finish_adaptive, detector_gate).
3. **Paired law** — OG + remint graded on the SAME two vendors, same day,
   per-image mapping; Δ is the primary metric.
4. **Corpus law** — all experiments grade on the fixed 20-image registry;
   OG baselines recorded once, reused forever.
5. **Decision-provenance law** — every routing decision recorded as
   `routing_decision {rule_version, inputs, chosen, reason}`.
6. **QA law** — borderline outputs flagged for manual QA, never shipped
   silently.
7. **Rubric law** — every batch carries the human 100%-zoom rubric; the
   operator's conversion verdict is a first-class metric.

---

## 6. CONSTRAINTS (frozen unless you argue with data)

- Stage-one CODE frozen; Track F = runtime routing only. Any stage-one code
  change needs a detector A/B on the full registry + owner sign-off.
- Finisher: non-generative, deterministic, CPU-only, one encode, sub-second
  at 1080p (~340–670 ms today).
- Ship-gate thresholds frozen (ai ≤ 0.45, flux-family ≤ 0.30, deepfake
  ≤ 0.10); new diagnostics are report-only.
- Knobs stay: presets + dither/smoothing/sharpen multipliers + wall toggle
  + delivery scale.
- All new finisher stages: constants + report metrics first; default-ON
  only after a positive registry A/B.
- GPU budget unchanged: warm ComfyUI stage untouched; wash-variant sweeps
  reuse the warm workflow with runtime parameters.

## 7. WHAT WE DO NOT NEED

Product, pricing, or business discussion. Pure technical system and design
recommendations. End with a build order an engineer can execute directly,
plus the exact split-test data you still need if the matrix is
insufficient. End with one of: `READY_NEEDS_OWNER_RUN` ·
`READY_FOR_OWNER_REVIEW` · `BLOCKED` + reason.
